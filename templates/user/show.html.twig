{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    

    
    {% if app.user %}
    <h1>{{ user.username }}</h1>
    <table class="table">
        <tbody>
{#             <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr> #}
                <th>Username</th>
                <td>{{ app.user.username }}</td>
            </tr>
            <tr>
                <th>Bio</th>
                <td>{{ user.bio }}</td>
            </tr>
{#             <tr>
                <th>IsVerified</th>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
            </tr> #}
        </tbody>
    </table>
    {% else %}
    <p>Veuillez vous créer un profil</p>
{% endif %}

{% endblock %}
